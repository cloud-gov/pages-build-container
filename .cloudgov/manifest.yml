---    
applications:
  - name: ((name))
    no-route: true
    health-check-type: process
    instances: 0
    docker:
      image: ((image))
    services:
      - federalist-((env))-rds
      - federalist-((env))-uev-key
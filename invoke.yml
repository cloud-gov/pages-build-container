# Configuration for invoke
#
# Ref: http://docs.pyinvoke.org/en/latest/concepts/configuration.html

run:
    # Always replace the environment with either the specified
    # env kwarg, which defaults to an empty dict
    replace_env: true
    env:
        # This needs to be set, otherwise sometimes
        # printing strings from python will fail with a weird
        # ascii codec encoding error.
        PYTHONIOENCODING: 'UTF-8'
